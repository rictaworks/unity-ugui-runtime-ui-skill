# 実装報告テンプレート

このテンプレートは `unity-ugui-runtime-ui` スキルの実装報告（F6：報告／requirements.md 5.7節）が出力する固定構造である。
見出しの順序・文言はホストによらず同一とする。`< >` で囲んだ箇所を実際の値に置き換え、記入例の行はそのまま出力に残さない。

---

## 対象と模式図

対象画面・モード（構築／改修／レビュー）と、画面のテキストによる配置図を記す。

- 対象画面：`<画面名。例：InventoryScreen（所持アイテム一覧）>`
- モード：`<構築 | 改修 | レビュー>`
- 模式図：

```
<例：
+--------------------------------------+
| Header: タイトル             [Close] |
+--------------------------------------+
| ScrollRect (Viewport / Content)       |
|  [Row] [Row] [Row] ...                |
+--------------------------------------+
| Footer: [Cancel]         [Confirm]    |
+--------------------------------------+
>
```

---

## 前提と既定値

前提セット（requirements.md 5.2節）を、項目・値・出所（読取／既定／利用者指定）の一覧で記す。既定を採用した項目は理由を付さず既定であることのみ示す。

| 項目 | 値 | 出所 |
|---|---|---|
| プロジェクト有無 | `<例：Assets/・ProjectSettings/ともに存在>` | `<読取>` |
| Unityバージョン | `<例：6000.0.23f1>` | `<読取>` |
| 既存UI規約 | `<例：UiFactory.cs 検出。命名・配置はこれに従う>` | `<読取>` |
| 文字体系 | `<例：日本語（CJK）を含む>` | `<読取>` |
| テキスト方式 | `<例：UnityEngine.UI.Text（TMPフォント未整備のため）>` | `<読取>` |
| 参照解像度 | `<例：1920×1080>` | `<既定>` |
| 入力方式 | `<例：マウス＋タッチ>` | `<既定>` |
| 検証段階 | `<例：CLI段階（UNITY_PATH設定済み） または 静的検査段階>` | `<読取>` |

---

## UX判断

レイアウト方式・状態・フィードバック・導線・追従規則・アクセシビリティ判定（requirements.md 5.3節）を1項目1行で記す。

- レイアウト方式：`<例：一覧はScrollRect（Viewport→Content、VerticalLayoutGroup＋ContentSizeFitter）>`
- 状態一覧：`<例：初期／読込中／空／エラー／無効／成功の6状態をRender(state)で分岐>`
- フィードバック：`<例：ボタンは押下時に拡縮、削除操作は確認ダイアログを挟む>`
- 導線：`<例：ヘッダー右上に閉じるボタン、背景タップでも閉じる>`
- 追従規則：`<例：CanvasScaler=ScaleWithScreenSize、参照解像度1920×1080、マッチ=横長0.5/縦長0>`
- アクセシビリティ判定：`<例：タップ領域48px四方、本文16px、前景/背景コントラスト比4.8（判定基準4.5以上を満たす）>`

---

## 生成・変更ファイル

生成・変更したファイルを一覧にする。改修モードでは、要求範囲内の変更と、要求範囲外で見つかった不変条件違反等の所見を分けて記す。

### 生成・変更（要求範囲内）

| ファイル | 種別 | 概要 |
|---|---|---|
| `<例：Assets/Scripts/UI/Screens/InventoryScreen.cs>` | `<新規>` | `<例：所持アイテム一覧画面のBuild／Render>` |
| `<例：Assets/Scripts/UI/UiTheme.cs>` | `<変更なし（既存流用）>` | `<例：色・寸法定義は既存を参照>` |

### 変更範囲外の所見（改修モードのみ・変更していない）

- `<例：同一ディレクトリのShopScreen.csにGameObject.Findによる再取得を検出したが、要求範囲外のため変更していない>`

---

## 検証段階と結果

実施した検証段階（requirements.md 5.6節）と結果を記す。未実施の段階は「未実施」と明記し、実施済みとして報告しない。

| 段階 | 実施 | 結果 |
|---|---|---|
| 静的検査（lint_ugui_csharp.py） | `<実施>` | `<例：禁止パターンなし>` |
| コントラスト検査（check_contrast.py） | `<実施>` | `<例：全組み合わせが4.5以上>` |
| CLIコンパイル（unity_batch_compile.py） | `<実施 または 未実施（UNITY_PATH未設定）>` | `<例：error CSなし、再実行0回>` |
| 解像度スイープ（UiResolutionSweepTests） | `<実施 または 未実施（CLI段階のみ）>` | `<例：16:9/4:3/21:9/9:16ではみ出しなし>` |

---

## 既知の制限

今回の実装で解消されていない制限を記す（例：CLI未実施、フォント未同梱、プール未実装など）。

- `<例：UNITY_PATHが未設定のためCLIコンパイル・解像度スイープは未実施。静的検査のみで報告している>`
- `<例：一覧が50件を超える場合の行プールは今回未実装>`

---

## 範囲外の所見

要求文に含まれていたが本スキルの対象外（UI Toolkit・Editor拡張・IMGUI・TMPフォント生成等、requirements.md 2.2節）である部分を記す。該当がなければ「該当なし」と記す。

- `<例：「設定画面をUI Toolkitでも用意したい」という要求は範囲外のため未対応。uGUIでの実装のみ行った>`
- `<該当なしの場合：範囲外の所見はなし>`
