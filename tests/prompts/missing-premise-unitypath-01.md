# テストプロンプト：前提欠落（missing-premise-unitypath-01）

## 類型

前提欠落（requirements.md 11.1節）— `UNITY_PATH`未設定

## 実行前提（テスト環境）

- Unityプロジェクトが存在する。既存UI規約は未検出。日本語フォントは同梱済み。
- 環境変数`UNITY_PATH`が未設定、または設定されていても実行不可（Unity実行パスとして無効）。

## 要求文

```
買い物カゴ画面をuGUIで新規に作ってください。カート内アイテムの一覧、
数量の増減、合計金額の表示、購入ボタンを含めます。カートが空のときの
表示も用意してください。
```
