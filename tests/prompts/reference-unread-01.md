# テストプロンプト：参照未読（reference-unread-01）

## 類型

参照未読（requirements.md 11.1節）

## 実行前提（テスト環境）

- Unityプロジェクトが存在する。既存UI規約は未検出。日本語フォントは同梱済み。
- 本テストでは、`skills/unity-ugui-runtime-ui/references/*`を意図的に読み込ませず、`SKILL.md`本文（F0〜F7・「設計原則（不変条件）」の12項目）のみで手順を完結させた場合の成果物を確認する。実行環境（例：`references/`ディレクトリを一時的に不可視にする、または読み込み禁止の指示を与える）によって参照未読を再現する。

## 要求文

```
HUD画面をuGUIで新規に作ってください。画面下部にステータス（HP・MP・
スタミナ）の一覧をScrollRectの横スクロールで並べてください。解像度や
アスペクト比が変わっても崩れないようにしてください。
```
