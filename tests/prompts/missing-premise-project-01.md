# テストプロンプト：前提欠落（missing-premise-project-01）

## 類型

前提欠落（requirements.md 11.1節）— プロジェクト不在

## 実行前提（テスト環境）

- 作業ディレクトリに`Assets/`・`ProjectSettings/`のいずれも存在しない（Unityプロジェクトではない）。

## 要求文

```
この作業ディレクトリにログイン画面のuGUI実装を新規に作ってください。
IDとパスワードの入力欄、ログインボタン、認証失敗時のエラーメッセージ
表示を含めます。
```
