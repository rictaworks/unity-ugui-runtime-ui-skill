# 期待結果：非発火（no-fire-02）

対応プロンプト：`tests/prompts/no-fire-02.md`

## 発火可否

**発火しない。**

- 一覧・無限スクロール・状態表示（読込中・空・エラー）は`fire-build-01`のuGUI要求と表面的な構造は似ているが、Unity・uGUI・Canvas・RectTransform・ScrollRect・Button・WebGLのいずれにも言及していない。3.1節「どの文脈で使うか」の条件語が1つも現れないため`description`は反応しない。
- 3.3節「発火すべきでない要求の類型」の「Web・ネイティブ・他エンジンのUI」に該当する。

## モード判定（F0）

該当なし。要求文全体が対象外であり、F0手順1で適用部分が空と判定される（万一評価対象になった場合）。

## 停止可否

F0での適用取り下げに相当する。F1の停止条件（プロジェクト判定・文字体系ゲート等）は評価対象にすらならない。

## 期待される報告構造

7見出し構造は出力されない。本スキルが関与すべき要求ではない旨のみ（あるいは無応答／通常の実装フローに委ねる）。

## 生成ファイル名（期待）

なし。
