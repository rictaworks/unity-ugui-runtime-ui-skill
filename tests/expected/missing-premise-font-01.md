# 期待結果：前提欠落（missing-premise-font-01）

対応プロンプト：`tests/prompts/missing-premise-font-01.md`

## 発火可否

**発火する。** メニュー画面の新規uGUI構築要求であり対象範囲内（3.2節）。

## モード判定（F0）

**構築。**

## 前提収集（F1）の判定と停止可否

- F1手順1〜4：プロジェクトは存在し、既存UI規約は未検出、テキスト方式は`UnityEngine.UI.Text`が候補になる。
- F1手順5「文字体系ゲート」：要求文に日本語（CJK）文字（「設定」「サウンド」「ことば」等）が含まれる。`Resources/`配下にCJK対応フォントファイルもTMPフォントアセットも存在しないため、**この時点で停止する**。
- 組み込みの`LegacyRuntime.ttf`はCJKを含まずWebGLではOSフォントへのフォールバックが無いため、代替せずに停止する（不変条件7）。

**停止する。**

## 停止時の応答内容（期待）

- フォントファイル（ライセンス確認済みのもの）を`Assets/Resources/Fonts/`へ配置することを求める1〜数文の応答。
- F2以降（UX設計・コード構造設計・実装生成・検証）は実行されない（F1手順8）。

## 期待される報告構造

`assets/report-template.md`の7見出しをすべて満たした完全な実装報告は**出力されない**。停止理由（CJKフォント未同梱）と対応依頼のみで完結する。

## 生成ファイル名（期待）

なし。C#ファイルは一切生成されない。

## 検証観点

- 「日本語フォントが無いので既定の`LegacyRuntime.ttf`で代替して進める」という代替対応が行われていないこと（不変条件7の厳守）。
- 停止後、利用者がフォントを配置すれば処理を再開できる形の依頼文になっていること。
