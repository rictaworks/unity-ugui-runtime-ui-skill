# 期待結果：発火・改修（fire-modify-01）

対応プロンプト：`tests/prompts/fire-modify-01.md`

## 発火可否

**発火する。** 「uGUI」「レイアウト崩れ」「押しづらさ」「解像度・アスペクト比」に相当する語を含み、既存画面の操作性・崩れの改善という3.2節の類型（「既存のuGUI画面の操作性・見た目・レイアウト崩れ・押しづらさを改善する」）に該当する。原因特定と修正が単一手順では完了しない。

## モード判定（F0）

**改修。** 既存画面`ShopScreen.cs`への言及があり、コード評価（レビュー）の要求ではないため。範囲外部分はなし。

## 停止可否

**停止しない。** プロジェクト・文字体系ゲート等、前提収集の停止条件（F1手順5）に該当する記述がない。

## 変更範囲の限定（F3手順8・不変条件11）

- 変更対象：`ShopScreen.cs`の購入ボタン周辺のレイアウト・アンカー・タップ領域のみ。
- 要求外の`InventoryScreen.cs`にある不変条件違反（`GameObject.Find`再取得）は変更せず、報告の「変更範囲外の所見」に記載するにとどめる。全面書き換えは行わない。

## 期待される報告構造（F6）

7見出しは`fire-build-01`と同一順序。「生成・変更ファイル」は以下の2区分に分かれる：

- 生成・変更（要求範囲内）：`ShopScreen.cs`（変更）
- 変更範囲外の所見（改修モードのみ・変更していない）：`InventoryScreen.cs`の`GameObject.Find`再取得についての指摘

## 生成ファイル名（期待）

新規ファイルは生成されない（共通基盤・画面クラスとも既存を前提とするため）。変更は`Assets/Scripts/UI/Screens/ShopScreen.cs`のみ。

## 検証観点（不変条件との対応）

- 不変条件11：改修モードでは要求範囲外を変更せず所見として報告する。
- 不変条件10：タップ領域は参照解像度で44px四方以上に修正される。
- 追従規則（5.3節手順7）：アンカー・マッチ方式の見直しにより縦持ちでの画面外はみ出しが解消される。
- 検証段階と結果に、修正後の静的検査結果（禁止パターンなし）が明記される。

## 参照ファイル読み込み（期待）

レイアウト崩れの改修のため`references/layout.md`が読み込まれる。ボタン（Selectable）の押下遷移に触れる場合は`references/components.md`も対象になり得る。
